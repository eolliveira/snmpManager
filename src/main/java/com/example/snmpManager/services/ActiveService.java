package com.example.snmpManager.services;

import java.util.List;

import javax.swing.JOptionPane;
import javax.transaction.Transactional;

import com.example.snmpManager.entities.Active;

public class ActiveService {
	
	
//
//	 @Transactional
//	    public List<Active> findById(String address){
//	        List<Active> activies = (categoryId == 0) ? null : Arrays.asList(categoryRepository.getOne(categoryId));
//	        Page<Product> page = repository.find(categories, name, pageable);
//	        repository.productsByCategory(page.getContent());
//	        return page.map(p -> new ProductDTO(p, p.getCategories()));
//	    }
	
	
	

}
